var a = "Hello World"
var b = "I have something to tell you"

print(a + b)

function baseline(n) {
    var start = time()
    for i, n {}
    return time()-start
}

var c = ""

var n = 5000000

var base = baseline(n)

print("Empty: " + base + " ms")

while true {

    var start = time()

    for i, n {
        c = a + b
    }

    print( (time() - start - base) + " ms" )
}